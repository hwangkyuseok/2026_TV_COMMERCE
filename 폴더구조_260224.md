2026_TV_COMMERCE/
├── docker-compose.yml          # Front(Next.js), Back(FastAPI), DB(PostgreSQL) 배포용
├── CLAUDE.md                   # 클로드 AI 코딩 지시서 (프로젝트 헌법)
├── mock_data.json              # 초기 테스트용 더미 데이터
├── .gitignore
├── .env.example                # 환경변수 템플릿
├── README.md
│
├── frontend/                   # 📺 Next.js 프론트엔드 (TV 앱)
│   ├── public/                 # 정적 리소스 (이미지, 비디오 소스 등)
│   ├── src/
│   │   ├── app/                # Next.js App Router (page.tsx, layout.tsx, globals.css)
│   │   ├── components/
│   │   │   ├── common/         # 공통 UI (PurchaseModal.tsx, ConsultModal.tsx)
│   │   │   └── tv/             # TV 특화 UI (Sidebar.tsx, VideoPlayer.tsx, Carousel.tsx,
│   │   │                       #             ShoppingRow.tsx, ShoppingGrid.tsx, ProductList.tsx)
│   │   ├── constants/          # 상수 관리 (keyCodes.ts - 리모컨 방향키 매핑)
│   │   ├── hooks/              # 커스텀 훅 (useRemoteFocus.ts)
│   │   └── types/              # 타입스크립트 인터페이스 (index.ts)
│   ├── Dockerfile
│   ├── tailwind.config.ts
│   └── package.json
│
├── backend/                    # ⚙️ FastAPI 백엔드 & Vision AI 서빙
│   ├── app/
│   │   ├── main.py             # 앱 엔트리 포인트 및 전역 Exception Handler
│   │   ├── api/
│   │   │   └── v1/             # API v1 라우터 (mock_api.py, vision.py 등)
│   │   ├── core/               # 앱 설정 및 인프라
│   │   │   ├── config.py       # 환경변수 및 설정값 관리 (pydantic-settings)
│   │   │   └── scheduler.py    # 주기적 작업 스케줄러 (APScheduler 등)
│   │   ├── models/             # ML/DL 모델 클래스 (YOLO 추론 래퍼, 객체인식 모델 등)
│   │   ├── schemas/            # Pydantic 입출력 스키마 (product.py 등)
│   │   ├── repositories/       # DB 접근 레이어 - Repository 패턴 (product_repository.py)
│   │   ├── services/           # 핵심 비즈니스 로직 (scraper_service.py 등)
│   │   └── utils/              # 공통 유틸리티
│   │       ├── image_utils.py  # 이미지 처리 유틸 (리사이즈, Base64 변환 등)
│   │       └── logger.py       # 공통 로거 설정
│   ├── ml_weights/             # YOLO 모델 가중치 파일 (.pt, .onnx) - models/ 에서 로드
│   ├── Dockerfile
│   └── requirements.txt
│
├── document/                   # 📄 프로젝트 산출물 및 설계 문서
│   ├── ddl.sql                 # DB 테이블 DDL 정의
│   └── scraping_requirements.md # 스크래핑 요구사항 정의서
│
├── HelloVision/                # 🔬 레퍼런스 데이터 및 Vision 실험용
│   ├── img/                    # 테스트용 이미지 샘플
│   ├── log/                    # 실험 로그
│   ├── sample_data.json        # 샘플 데이터
│   └── table_schema.md         # 테이블 스키마 레퍼런스
│
└── scripts/                    # 🛠️ 자동화 스크립트 (크롤러, ONNX 변환 등)
