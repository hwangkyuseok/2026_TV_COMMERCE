2026_TV_COMMERCE/
├── .venv/                      # 로컬 파이썬 가상환경 (백엔드 및 모델 변환용)
├── .gitignore                  
├── docker-compose.yml          # Front(Next.js), Back(FastAPI), DB(PostgreSQL) 띄우기용
├── claude.md                   # ★ 클로드 AI 코딩 지시서 (위의 마크다운 내용)
├── README.md
├── docs/                       # 기획서, 요구사항 정의서, API 명세서 보관
│
├── frontend/                   # 📺 Next.js 프론트엔드
│   ├── public/                 
│   ├── src/
│   │   ├── components/         # TV용 캐러셀, 그리드, 모달 UI 컴포넌트
│   │   ├── hooks/              # ★ useRemoteFocus.ts (리모컨 제어 핵심 로직)
│   │   ├── app/                # Next.js App Router 페이지
│   │   └── api/                # 백엔드 연동용 Fetch 함수들
│   ├── Dockerfile
│   └── package.json
│
├── backend/                    # ⚙️ FastAPI 백엔드 & Vision AI 서빙
│   ├── app/
│   │   ├── main.py             # 앱 엔트리 포인트 및 전역 Exception Handler
│   │   ├── api/
│   │   │   ├── endpoints/      # 라우터 (recommendations.py, vision.py, orders.py)
│   │   ├── core/               # DB 연결, 로거 설정 (logger.py), 환경변수
│   │   ├── models/             # SQLAlchemy ORM 스키마 (Products, Users, Logs)
│   │   ├── services/           # 비즈니스 로직 및 YOLO 객체 인식 추론 (detector.py)
│   │   └── utils/              # 공통 유틸 (문자 발송, 이미지 전처리 등)
│   ├── ml_weights/             # 모델 가중치 폴더 (Git LFS 관리 대상)
│   │   ├── yolov11n.pt         # 학습 완료된 원본 가중치
│   │   ├── yolov11n.onnx       # ★ 실제 서빙에 사용될 변환된 가중치
│   │   └── classes.json        # 클래스 매핑 (ex: 0: "TV", 1: "Jacket")
│   ├── Dockerfile
│   └── requirements.txt        # fastapi, onnxruntime, sqlalchemy, psycopg2 등
│
└── scripts/                    # 🛠️ 자동화 및 데이터 전처리 스크립트
    ├── crawler.py              # 렌탈 쇼핑몰 상품 정보 수집용
    └── export_to_onnx.py       # YOLO .pt 모델을 .onnx로 변환하는 스크립트