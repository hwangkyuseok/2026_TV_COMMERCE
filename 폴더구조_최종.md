2026_TV_COMMERCE/
├── docker-compose.yml          # Front(Next.js), Back(FastAPI), DB(PostgreSQL) 배포용
├── claude.md                   # 클로드 AI 코딩 지시서 (프로젝트 헌법)
├── mock_data.json              # 초기 테스트용 더미 데이터
├── .gitignore                  
├── README.md
│
├── frontend/                   # 📺 Next.js 프론트엔드 (TV 앱)
│   ├── public/                 # 정적 리소스 (이미지, 비디오 소스 등)
│   ├── src/
│   │   ├── app/                # Next.js App Router (page.tsx, layout.tsx, globals.css)
│   │   ├── components/
│   │   │   ├── common/         # 공통 UI (PurchaseModal.tsx, ConsultModal.tsx 등)
│   │   │   └── tv/             # TV 특화 UI (Sidebar.tsx, VideoPlayer.tsx, ShoppingRow.tsx, ShoppingGrid.tsx, Carousel.tsx)
│   │   ├── constants/          # 상수 관리 (keyCodes.ts - 리모컨 방향키 매핑 등)
│   │   ├── hooks/              # 커스텀 훅 (useRemoteFocus.ts)
│   │   ├── types/              # 타입스크립트 인터페이스 (index.ts)
│   │   └── api/                # 백엔드 연동용 Fetch 함수들
│   ├── Dockerfile
│   └── package.json
│
├── backend/                    # ⚙️ FastAPI 백엔드 & Vision AI 서빙
│   ├── app/
│   │   ├── main.py             # 앱 엔트리 포인트 및 전역 Exception Handler
│   │   ├── api/
│   │   │   └── endpoints/      # API 라우터 (mock_api.py, vision.py 등)
│   │   ├── core/               # 환경변수, 로거, AI 설정 관리
│   │   ├── models/             # DB ORM 스키마 및 Pydantic 데이터 모델
│   │   ├── services/           # 핵심 비즈니스 로직 및 YOLO 추론 로직 분리
│   │   └── utils/              # 공통 유틸리티
│   ├── ml_weights/             # YOLO 모델 가중치 폴더 (.pt, .onnx)
│   ├── Dockerfile
│   └── requirements.txt
│
└── scripts/                    # 🛠️ 자동화 스크립트 (크롤러, ONNX 변환 등)